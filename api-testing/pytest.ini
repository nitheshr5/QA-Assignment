[pytest]
markers =
    api: API tests for the Reqres demo service
    positive: Tests that validate successful flows
    negative: Tests that validate error handling behavior
    smoke: Small, fast subset of tests for quick verification
